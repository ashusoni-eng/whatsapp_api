diff --git a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
index 540078a..3bf0ccd 100644
--- a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
+++ b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
@@ -483,6 +483,12 @@ exports.LoadUtils = () => {
             ...extraOptions,
         };
 
+        // MediaData is a model whose private __x_id field collides with Msg's
+        // internal id field when its enumerable properties are spread above,
+        // breaking getValidatedSender() during Msg initialization.
+        // Upstream fix: https://github.com/wwebjs/whatsapp-web.js/pull/201923
+        delete message.__x_id;
+
         // Bot's won't reply if canonicalUrl is set (linking)
         if (botOptions) {
             delete message.canonicalUrl;
